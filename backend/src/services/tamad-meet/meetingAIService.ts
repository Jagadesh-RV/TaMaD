export const processMeetingTranscript = async (meetingId: string, transcriptText: string) => {
  // Implementation for AI summary pipeline
  return { summary: 'Mock AI Summary', actionItems: [] };
};
